Config = {}

-- Below is an example of a "tape" that corresponds with a file in your chHyperSound `sounds` directory. The key of these table entries is the itemname associated with items in your shared list. 
-- The artist and song are the details of that song that will be displayed in the menu.
-- The file is the filename without the extension of the song, in the `sounds` folder.
-- The song will only be available in the boombox menu if your are holding the tape in your inventory.

Config.SongsList = {
    -- ["boombox_tape_branchez"] = { artist = "Branchez", song = 'Stay (Branchez Bootleg)', file = 'branchezstay'},
}